# Lab 教学性评审（src/components/lab）

本文件用于回答一个问题：每个 Lab 组件的功能是否“能达成学习目的”，还是“仅展示、缺少教学闭环”。评审以代码结构为准（是否有目标、引导、练习/自测、与真实项目的迁移），而不是以 UI 丰富程度为准。

## 评审标准（四件套）

- 目标：学完应能解释什么、做出什么
- 可操作：有交互演示/练习区，把概念落地
- 可验证：有自测/验收标准（可判断对错）
- 可迁移：能对照本站真实代码，并带问题阅读（观察点）

## 总体结论

- 教学内容本身并不少：多数 `Lab*.vue` 已包含讲解结构与交互演示，尤其是基础阶段（HTML/JS/CSS）更接近“可用课程”。
- “工具/容器/索引”类文件不承载教学内容，但对学习体验必要：它们属于学习平台能力，不应被当作“无意义展示”。
- 主要短板是：部分演示型 Lab 缺少统一的任务闭环（迁移题 + 自测 + 解析），导致学习效果依赖自觉。

## 组件分层（先把“教学内容”与“平台能力”分开看）

### 1) 学习平台能力（支撑学习，不直接讲概念）

- 容器/布局：`PanelContent.vue`、`DualColumnView.vue`
- 源码阅读与笔记：`SourceCodeViewer.vue`、`SourceFileTree.vue`、`CodeMinimap.vue`
- 进度与导航：`LabDashboard.vue`、`dashboard/*`、`useDirectoryFollow.ts`、`useStagePaging.ts`
- 组件索引导出：各 `index.ts`

判断：这些组件本身不需要“教学化”，但可以通过“引导如何使用”提升学习效率（首次提示、推荐阅读路径、带问题阅读等）。

### 2) 教学内容组件（应达成学习目标）

#### 强闭环（讲解 + 交互 + 练习/自测）

- HTML：`stage1-foundation/LabHtmlBasics.vue`（理论/练习/示例/测验结构完整）
- JS：`stage2-js-basics/LabJsBasics.vue`（Why + 速查 + 分主题交互 + 项目对照）
- CSS：`stage3-css/LabCssBasics.vue`（Why + 速查 + 交互盒模型 + Tailwind 对照）

#### 中等闭环（有引导与演示，但可验证产出不稳定）

- 多数 `stage3-css/*`、`stage6-vue-core/*`、`stage7-vue-advanced/*`：通常具备讲解与演示，但需要更一致的“可判定自测/迁移题”。

#### 偏演示（有可视化，但缺少任务闭环）

- 以 `stage4-js-advanced/LabEventLoop.vue` 为代表：可视化效果好，但如果没有“预测题/自测/迁移任务/错因解析”，容易停留在观看层面。

## 已补强的闭环（本次改动）

- 为 `LabEventLoop.vue / LabAsync.vue / LabAjax.vue` 补齐：
  - 迁移题（把演示迁移成小业务/可复用函数）
  - 自测题（单选 + 解析）
  - “带问题阅读”对照源码入口（打开代码时显示观察点）
- 为全部 `stages/Stage*.vue` 补齐“目标与完成标准”卡片，确保阶段学习有验收边界。

## 后续建议（如果继续提升）

- 统一每个知识型 Lab 的结构：目标 → 演示 → 迁移题 → 自测 → 回链（笔记/源码/下一 Lab）。
- 自测题加入“错题回链”：错了就提供一键跳回对应演示区域或笔记章节。
- 把“完成标准”与进度系统打通：满足自测/迁移题的验收后再标记完成，而不是只靠手动点“已学完”。

