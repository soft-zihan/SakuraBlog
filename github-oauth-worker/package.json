{
  "name": "github-oauth-worker",
  "version": "1.0.0",
  "description": "GitHub OAuth token exchange proxy for SakuraBlog",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
